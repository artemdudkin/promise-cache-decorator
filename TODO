tests for storage which methods returns not Promise (esp. load)
tests for non-global storage etc.
lru cache invalidator

fix *.d.ts ("any" type for all)
clear() cannot clean all storages

? add settings: hasher for keys and serializer for values

? test bluebird, q, etc.
? run Fibonacci test from memoizee
? add stats (look at memoizee or mem)

fix windows 'npm run'
fix problem - "UnhandledPromiseRejectionWarning" @ rejected promise test
once-a-day validator pattern

Add storage/lowdb
Add tests for persistance adapters
comment code (index.js)
